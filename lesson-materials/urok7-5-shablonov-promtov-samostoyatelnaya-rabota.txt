================================================================================
5 ШАБЛОНОВ ПРОМТОВ — самостоятельная работа (bCard, ~13 лет)
Заполни [КАПСОМ В СКОБКАХ] своими словами. Потом скопируй весь блок в ИИ.
Структуру не ломаем: только CSS, без .tsx, без .env, без новых пакетов.
================================================================================

ШАБЛОН 1 — палитра всей страницы (файл App.css, блок .page)
--------------------------------------------------------------------------------
Проект: React + Vite, визитка bCard.

Файл: my-app/src/App.css

Задача: только в селекторе .page измени CSS-переменные --bg, --panel, --panel-hover, --text-primary, --accent, --shadow на палитру в настроении: [НАПРИМЕР: ночной космос / мятная тетрадь / закат — НАПИШИ СВОЁ].

Запрещено: менять любые .tsx, main.tsx, vite.config.ts, .env; не переименовывать классы; не добавлять новые файлы и npm-пакеты.

Доступность: текст должен оставаться хорошо читаемым на фоне.

Формат ответа: только целиком правило .page { ... } для замены в файле, без длинных объяснений.


ШАБЛОН 2 — меню (файл App.css: .menu и ссылки)
--------------------------------------------------------------------------------
Проект: bCard, my-app/src/App.css.

Задача: измени только селекторы .menu, .menu a, .menu a:hover, .menu a:focus-visible: [НАПРИМЕР: больше скругление / мягче тень / больше отступы — НАПИШИ СВОЁ], не ломая читаемость ссылок.

Запрещено: менять display, flex-wrap, justify-content; не трогать .tsx и href в разметке.

Доступность: :focus-visible должен остаться заметным.

Формат ответа: только перечисленные CSS-правила подряд.


ШАБЛОН 3 — «воздух» у главного блока визитки (App.css: герой)
--------------------------------------------------------------------------------
Проект: bCard, my-app/src/App.css.

Задача: только у .heroSection и .content-row настрой padding, gap и при необходимости max-width так, чтобы на широком экране было [МЕНЬШЕ ПУСТОТЫ / АККУРАТНЕЕ ПО ЦЕНТРУ — НАПИШИ СВОЁ], не меняя сетку колонок.

Запрещено: менять flex-direction, align-items, width-логику колонок; не трогать .tsx.

Формат ответа: только изменённые правила .heroSection и .content-row целиком.


ШАБЛОН 4 — карточка формы «Написать мне» (ContactSection.module.css)
--------------------------------------------------------------------------------
Проект: bCard, my-app/src/components/ContactSection.module.css.

Задача: только селектор .formCard — фон, border-radius, box-shadow, border в стиле: [НАПРИМЕР: матовое стекло / бумажная карточка / неоновая рамка — НАПИШИ СВОЁ].

Запрещено: менять ContactSection.tsx; не менять имени классов в модуле; не трогать логику формы и Web3Forms.

Доступность: не снижай контраст текста внутри карточки.

Формат ответа: только правило .formCard { ... } целиком.


ШАБЛОН 5 — поля и кнопка формы (один файл, два шага по желанию)
--------------------------------------------------------------------------------
Проект: bCard, my-app/src/components/ContactSection.module.css.

Часть А — только .input, .textarea и ::placeholder для них: [СПОКОЙНЕЕ / ЯРЧЕ / МИНИМАЛЬНЕЕ — НАПИШИ СВОЁ] оформление рамки и фона. Сохрани или усиль заметный :focus-visible.

Часть Б (отдельным вторым сообщением в ИИ, если нужно): только .submit, .submit:hover, .submit:disabled — [ЦВЕТ / ГРАДИЕНТ / ПЛАВНЫЙ HOVER — НАПИШИ СВОЁ].

Запрещено: менять .tsx; не убирать type submit из разметки (tsx не трогать вообще).

Формат ответа: только указанные селекторы, без лишнего текста.

================================================================================
ПОСЛЕ ОТВЕТА ИИ — чеклист ученика
================================================================================
[ ] Вставил код только в названный файл
[ ] Сохранил файл
[ ] Обновил страницу в браузере (при необходимости Ctrl+F5)
[ ] Проверил: меню кликается, якоря работают, форма открывается
[ ] Не кидал в чат ключи и .env

================================================================================
Конец раздатки
================================================================================
