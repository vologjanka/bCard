================================================================================
ВСТАВКА 01 — константа адреса API Web3Forms
================================================================================

ГДЕ: в файле ContactSection.tsx сразу ПОСЛЕ строки с initialErrors, то есть
     после:
     const initialErrors: FormErrors = { name: '', email: '', message: '' }

     и ПЕРЕД функцией isValidEmail (или перед function isValidEmail).

ЧТО СДЕЛАТЬ: ВСТАВИТЬ блок ниже целиком (пустая строка после initialErrors
            не обязательна, но так читается приятнее).

ЗА ЧТО ОТВЕЧАЕТ: один адрес https://api.web3forms.com/submit — чтобы в fetch
не ошибиться в одной букве.



----- СКОПИРУЙТЕ ВЕСЬ БЛОК НИЖЕ -----

/** [УЧЕБА: Урок 6] Адрес API Web3Forms — менять только если сервис сменит URL в документации. */
const WEB3FORMS_SUBMIT_URL = 'https://api.web3forms.com/submit'

----- КОНЕЦ -----
