================================================================================
ВСТАВКА 03 — сделать handleSubmit асинхронным и сбрасывать submitError
================================================================================

ГДЕ: в начале функции handleSubmit в ContactSection.tsx.

ЧТО СДЕЛАТЬ — ДВА МАЛЕНЬКИХ ЗАМЕНЫ:

(А) Замените объявление функции. БЫЛО:
    const handleSubmit = (event: FormEvent<HTMLFormElement>) => {

    СТАЛО (добавилось слово async):
    const handleSubmit = async (event: FormEvent<HTMLFormElement>) => {

(Б) Сразу ПОСЛЕ строки event.preventDefault() вставьте ОДНУ новую строку:

    setSubmitError('')

ЗА ЧТО ОТВЕЧАЕТ:
• async — можно писать await fetch(...) и ждать ответ.
• setSubmitError('') — при каждом новом нажатии «Отправить» стираем старое
  красное сообщение про сеть/API.


----- СКОПИРУЙТЕ ТОЛЬКО ЭТУ СТРОКУ (вставить после event.preventDefault()) -----

    setSubmitError('')

----- КОНЕЦ -----

Также обновите комментарий НАД handleSubmit (необязательно, но полезно).
Можно заменить старый блок комментария на:

----- опционально: комментарий над функцией -----

  /**
   * [УЧЕБА: ШАБЛОН] Локальная валидация полей.
   * [УЧЕБА: Урок 6] После успешной валидации — POST на Web3Forms, затем очистка полей или сообщение об ошибке.
   */

----- конец опционального блока -----
